#include <iostream>

#include "solution.hpp"

// Optional demo driver. Delete this file if the subproject only needs tests.
int main() {
    std::cout << "2 + 3 = " << algo::add(2, 3) << '\n';
    return 0;
}
